# williamminer.com

## Branch workflow

- Create a feature branch before starting any new feature work.
- Start from a clean `main` (or `master`) working tree.
- Use this helper to enforce the flow:

`npm run branch:feature -- <short-feature-name>`

Example:

`npm run branch:feature -- snake-letter-targets`

## Up next

- [ ] add back bookmarks
