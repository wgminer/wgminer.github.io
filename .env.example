# Copy this file to ".env" and fill in your token (never commit .env).
#
# Raindrop → Settings → Apps (https://app.raindrop.io/#/settings/apps): open your
# app → copy **Test token** (non-expiring). Do not use Client ID or an expired OAuth
# access_token from the authorization flow.
RAINDROP_TOKEN=
