{
  "name": "wgminer.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node scripts/dev.js",
    "dev:bookmarks": "node build.js && live-server --port=8000 --open=index.html",
    "dev:bookmarks:watch": "concurrently -k -n build,www -c blue,green \"nodemon -r dotenv/config --watch .env --exec node build.js\" \"live-server --port=8000 --open=index.html\"",
    "bookmarks:watch": "nodemon -r dotenv/config --watch .env --exec \"node build.js\"",
    "branch:feature": "bash ./scripts/new-feature-branch.sh",
    "build:bookmarks": "node build.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.4.2",
    "live-server": "^1.2.2",
    "nodemon": "^3.1.14",
    "prettier": "^3.8.3"
  }
}
