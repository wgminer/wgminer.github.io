/**
 * Copy to config.js (gitignored). Prefer a root `.env` with RAINDROP_TOKEN for local dev.
 *
 * Token: Raindrop → Settings → Apps → **Test token** (not Client ID).
 * https://app.raindrop.io/#/settings/apps
 */
module.exports = {
  API_TOKEN: "",
};
